{{> header}}
<main>
    <img class="logoImg" src="/assets/img/pizzaclub_sm.jpg" alt="clublogo">
    <h2>{{{text}}} </h2>
    <p>if you're allready register please  <a href="/login">log-in</a></p>
      {{#if error}}
    <p class="error">{{error}} </p>
    {{/if}}
    {{> forms/reg}}

 
</main>
<script src="/pass-info.js"></script>
{{> footer}}