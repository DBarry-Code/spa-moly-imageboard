<div>
    <a class="singerLink" href="{{homepage}}" target="_blank">{{first_name}}
        {{last_name}}
    </a>

    <span>
        {{age}}
    </span>
    <a class="singerLink" href="/signatures/{{city}}">{{city}} </a>
</div>